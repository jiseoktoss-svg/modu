# modu 프론트엔드 구현 문서

## 1. 프론트엔드 목표

modu의 프론트엔드는 사용자가 30초 안에 서비스 가치를 이해하고, 복잡한 일정 조율 과정을 부담 없이 완료할 수 있도록 구성한다.

핵심 UX 목표:

- 주최자는 빠르게 회의를 만들고 링크를 복사할 수 있다.
- 참석자는 로그인 없이 모바일에서도 쉽게 시간표를 입력할 수 있다.
- 참석자는 모든 응답이 모인 뒤 후보 시간대에 투표할 수 있다.
- 주최자는 전체 캘린더, 추천 후보의 이유, 투표 현황을 한눈에 이해할 수 있다.
- 모든 상태는 색상뿐 아니라 텍스트 라벨로도 표현한다.

## 2. 기술 기준

- Next.js App Router
- TypeScript
- Tailwind CSS
- Server Components 기본 사용
- 상호작용이 필요한 폼, 시간표, 복사 버튼, 추천 재계산 UI는 Client Components로 분리
- 본문 텍스트 폰트는 `Pretendard`(globals.css의 CDN `@import` + `tailwind.config.ts`의 `fontFamily.sans`에 반영)
- 별도 아이콘 라이브러리를 두지 않는다. UI 아이콘은 유니코드 이모지를 `components/ui/Emoji.tsx`(`TossFace` 폰트)로 렌더링한다. (`lucide-react` 의존성 제거)
- 이모지는 `public/fonts/TossFaceFontWeb.otf`(COLR/CPAL, 전 브라우저 컬러)를 우선 사용하고 `public/fonts/TossFaceFontMac.ttf`로 폴백하며, `TossFace` 폰트의 `unicode-range`로 이모지 코드포인트에만 적용된다

## 3. 주요 라우트

| 경로 | 역할 |
| --- | --- |
| `/` | 랜딩 화면 |
| `/meetings/new` | 회의 만들기 |
| `/meetings/[meetingId]/share` | 회의 생성 후 공유 화면 |
| `/meetings/[meetingId]/share/[adminToken]` | 기존 링크 호환용 공유 화면 |
| `/m/[meetingId]` | 참석자 응답 화면 |
| `/admin/[meetingId]/[adminToken]` | 주최자 결과 화면 |
| `/meetings/[meetingId]/confirmed` | 확정 화면 |
| `/api/meetings/[meetingId]/ics` | `.ics` 다운로드 |

## 4. 화면별 구현 상세

### 4.1 랜딩 화면

필수 콘텐츠:

- 서비스명: `modu`
- 메인 카피: `모두의 시간, 모두의 회의`
- 서브 카피: `회의를 생성하여 참여자들에게 링크를 전달하세요.`
- CTA: `회의 만들기`

화면 방향:

- 첫 화면에서 제품명과 핵심 카피가 분명히 보여야 한다.
- 랜딩만 읽어도 단순 투표 앱이 아니라 의사결정 도구라는 점이 드러나야 한다.
- 마케팅 페이지처럼 과하게 장식하지 않고, 실제 서비스 진입을 우선한다.

### 4.2 회의 만들기 화면

주요 컴포넌트:

- `MeetingCreateForm`
- `ParticipantListEditor`

화면 톤:

- 페이지 배경은 `#fafaf8`이며, 카드 없이 라벨 텍스트 + 입력창으로 구성한다.

폼 기본값:

- 회의 길이: 1시간 0분
- 회의 마감 날짜: 오늘 기준 7일 뒤
- 내부 계산용 근무 시간: 09:00~18:00
- 신규 회의의 날짜 범위: 오늘부터 회의 마감 날짜까지
- 참석자: 비어 있는 상태로 시작

문장 빌더 UX(한 페이지 안의 단계형):

- 주최자가 항목을 하나씩 입력할수록 페이지 상단의 한국어 안내 문장이 완성되고, 하단에는 현재 항목 입력창과 `다음` 버튼이 위치한다.
- 상단 문장의 값을 클릭하면 그 항목으로 돌아가 수정할 수 있다.
- 아직 채우지 않았거나 지운 값 자리에는 회색 점 3개의 `파도타기`(dot-wave) 애니메이션을 표시한다.
- 새 절/단계는 fade 애니메이션으로 등장한다.
- 순서: 회의명 → 안건 → 장소 → 회의 마감 날짜 → 예상 회의 진행 시간(시간/분) → 참석자
- 참석자 단계는 `참석자 선택하기` 버튼으로 모달을 열고, 선택 후 상단 문장에 참석자 이름과 총 인원을 표시한다.
- 최종 문장 형태: `이번 회의명은 {회의명}에요. 회의 안건은 {안건} 입니다. 회의 장소는 {장소}이며, {날짜(요일 포함, 예: 2026년 7월 5일 일요일)}까지는 회의가 완료되어야 해요. 예상 회의 진행 시간은 {시간}시간 {분} 분입니다.` 그리고 줄을 바꿔 `회의 참석자 명단은 {이름1}, {이름2}, … 총 N명 입니다.` (시간 값이 0이면 시간 부분, 분 값이 0이면 분 부분을 문장에서 생략한다.)

입력 컨트롤:

- 회의 마감 날짜는 디자인 톤에 맞춘 커스텀 캘린더 `DatePicker`(요일 표시, KST 안전, `min=오늘`)로 받는다. `input[type=date]`는 사용하지 않는다.
- 회의 길이 입력 라벨은 `예상 회의 진행 시간을 입력해주세요`이며, 시간/분을 직접 입력한다.
- `Enter` 키는 다음 단계로 진행만 하고 폼을 제출하지 않는다. 제출은 `회의 만들기` 버튼으로만 한다.
- `회의 만들기` 제출 버튼은 참석자 선택을 완료(2명 이상 + 모달 닫음)했을 때만 노출한다.

검증(모든 항목 필수):

- 회의명, 안건, 장소, 회의 마감 날짜, 예상 회의 진행 시간이 모두 필수다.
- 참석자는 최소 2명 이상이어야 한다.
- 회의 길이는 시간과 분을 직접 입력하며, 합산 시간이 근무 시간(9시간) 이내여야 한다.

주최자는 근무 시작/종료 시간과 점심 시간을 입력하지 않는다. 화면에서는 회의 마감 날짜만 받고, 서버가 오늘부터 마감 날짜까지의 범위와 09:00~18:00 고정 근무 시간을 저장한다.

참석자 모달:

- 모달은 고정 크기 + 내부 스크롤(내용이 늘어도 크기 유지)이며, 바깥(backdrop) 클릭이나 `Esc`로 닫고, 열릴 때 포커스를 이동하고 focus trap을 둔다.
- 모달 상단에는 `참석자 선택` 타이틀만 두고, 별도 서브타이틀이나 설명 문구는 두지 않는다.
- 레이아웃은 위/아래 구성이다. 위에는 검색창(왼쪽 정렬)과 50명의 가상 직원 목록을 두고, 아래에는 `필수참석`·`선택참석` 두 구역을 둔다.
- 직원은 이름과 직책을 가지며, 이름 또는 직책으로 검색할 수 있다.
- 직원 목록 항목을 클릭하면 참석자를 추가/해제 토글한다.
- 선택된 참석자는 pill(둥근 뱃지) 형태로 아래 구역에 표시된다.
- 새로 추가하면 기본 `필수참석` 구역으로 들어간다(기본 `attendanceType=required`). 필수↔선택 이동은 뱃지 드래그 앤 드롭으로 한다.
- 빈 공간을 드래그하면 사각형(마퀴)으로 여러 명을 한 번에 선택한 뒤 함께 옮길 수 있다. (마우스/데스크톱 기준, 터치 미지원)
- 직원 목록 영역은 위/아래에 페이드를 둔다.
- 선택 인원 뱃지 문구는 `필수 N명 · 선택 M명`이다.

### 4.3 공유 화면

주요 컴포넌트:

- `ShareLinksPanel`
- `CopyButton`

표시 정보:

- 참석자용 공유 링크

복사 UX:

- 복사 성공 시 버튼 라벨을 잠시 `복사됨`으로 변경한다.
- 링크는 잘리지 않게 줄바꿈 가능한 형태로 보여준다.

### 4.4 참석자 응답 화면

주요 컴포넌트:

- 날짜 카드 UI(날짜별 가능/선호/불가능 토글)
- 공통 시간 일괄 적용(상태 + 시작~종료 시간 → 전체 날짜)
- 하단 고정 제출 버튼
- 제출 후 일주일 그리드 회의 캘린더
- `VotingPanel`

날짜 상태:

- `available`: 가능
- `busy`: 불가능
- `preferred`: 선호

응답 방식:

- 오늘부터 회의 마감 날짜까지 날짜 카드를 보여준다.
- 각 날짜는 전체 `가능`, `선호`, `불가능`으로 빠르게 지정할 수 있다.
- `공통 시간 일괄 적용`에서 상태와 시작~종료 시간을 고르면 모든 날짜의 해당 시간대에 한 번에 반영된다.
- 선택하지 않은 시간대는 기본적으로 `가능`으로 본다.

중요한 데이터 정책:

- `available`은 기본 상태이며 DB에 저장하지 않는다.
- 새 응답 플로우에서는 변경된 `preferred`, `busy` 시간 블록만 서버에 제출한다.
- 기존 데이터 호환을 위해 서버와 추천 알고리즘은 `avoid` 상태도 읽을 수 있다.

투표 UX:

- 응답 제출 후 같은 브라우저에서는 `participantToken`으로 본인 응답을 수정할 수 있다.
- 응답 제출 후에는 단순 완료 화면이 아니라 일주일 그리드 회의 캘린더를 보여준다.
- 회의 캘린더는 날짜 컬럼 × 30분 시간 행 그리드이며, 화면을 넓게(대시보드) 쓰고 마우스 드래그·휠로 가로 스크롤한다. 시간(KST) 축은 좌측에 고정한다.
- 각 칸은 `선호`(초록)·`불가능`(빨강) 그룹으로 참석자 이름 pill 을 직접 표시하고, 이름 색은 참석자별로 다르다. 가능·미응답은 표시하지 않는다.
- 모든 시간 칸은 동일한 높이를 유지한다.
- 캘린더에는 응답 수정 액션을 제공한다.
- 모든 참석자가 응답하기 전에는 후보 투표 대신 대기 안내를 보여준다.
- 모든 참석자가 응답하면 추천 후보 목록을 보여주고, 참석자는 후보 중 하나에 투표한다.
- 같은 참석자가 다시 투표하면 기존 투표를 새 후보로 교체한다.

모바일 UX:

- 각 셀의 터치 영역은 충분히 커야 한다.
- 상태 선택은 세그먼트 컨트롤 또는 하단 고정 툴바로 제공한다.
- 날짜가 많을 경우 날짜 카드는 2열 또는 1열 반응형 그리드로 표시한다.
- 날짜별 세부 시간은 `공통 시간 일괄 적용`으로 한 번에 지정한다.
- 제출 후 캘린더는 모바일에서도 가로 스크롤을 허용하되, 시간 축은 고정해 현재 행의 의미를 잃지 않게 한다.

수정 UX:

- 첫 제출 후 `participantToken`을 localStorage에 저장한다.
- 같은 브라우저에서 다시 접속하면 기존 응답을 불러와 수정 가능 상태로 보여준다.
- 토큰이 없거나 일치하지 않으면 새 응답 또는 본인 선택 흐름으로 유도한다.
- 이미 제출한 참석자가 같은 브라우저로 접속하면 완료 카드가 아니라 회의 캘린더 화면을 바로 보여준다.

### 4.5 주최자 결과 화면

주요 컴포넌트:

- `AdminMeetingHeader`
- `ParticipantResponseList`
- `AvailabilitySummaryCalendar`
- `RecommendationList`
- `RecommendationCard`
- `AttendanceTypeToggle`
- `ConfirmSlotDialog`

추천 카드 정보 위계:

1. 시간
2. 추천 등급
3. 필수 참석자 충족 여부
4. 선택 참석자 참석 가능 수
5. 충돌/비선호 요약
6. 추천 이유
7. 참석자별 영향 요약
8. 후보별 투표 수

추천 등급 예시:

- `가장 추천`
- `추천`
- `조건부 추천`
- `주의 필요`

상호작용:

- 필수/선택 참석 여부를 변경하면 추천 결과를 즉시 다시 계산한다.
- 변경은 서버에 저장한 뒤 최신 데이터로 추천을 다시 그린다.
- 모든 참석자의 투표가 모인 뒤 최다 득표 후보에 확정 버튼을 보여준다.
- 1위가 동률이면 동률 후보들에 확정 버튼을 보여주고, 주최자가 하나를 골라 확정한다.
- 후보 확정 전 확인 다이얼로그를 보여준다.

### 4.6 확정 화면

주요 컴포넌트:

- `ConfirmedMeetingSummary`
- `ShareSummaryText`
- `CopyButton`
- `IcsDownloadButton`

표시 정보:

- 확정된 회의 시간
- 회의 길이
- 참석 대상 요약
- 필수 참석자 충족 여부
- 공유용 문구
- `.ics` 다운로드 버튼

## 5. UI 스타일 방향

modu는 한국어 협업 도구처럼 간결하고 신뢰감 있게 만든다.

디자인 원칙:

- 넓은 여백
- 명확한 카드 구조
- 큰 타이포그래피
- 부드러운 라운드
- 쉬운 문장
- 모바일/데스크톱 반응형
- 색상에만 의존하지 않는 상태 표현
- 모달은 화면 목적을 나타내는 타이틀만 두고, 불필요한 서브타이틀이나 설명 문구를 넣지 않는다.

토스 제품처럼 쉽고 명확한 인상을 참고하되, 토스 브랜드, 로고, 고유한 시각 요소를 직접 모방하지 않는다.

추천 카드가 가장 중요한 UI다. 화면 전체가 추천 후보를 빠르게 비교하는 데 집중되어야 한다.

## 6. 컴포넌트 구조

권장 구조:

```txt
components/
  meeting/
    MeetingCreateForm.tsx
    ParticipantListEditor.tsx
    ShareLinksPanel.tsx
    ConfirmedMeetingSummary.tsx
  scheduler/
    ResponseForm.tsx      # 응답 입력, 제출 후 캘린더, 후보 투표
    AvailabilitySummaryCalendar.tsx
    RecommendationCard.tsx
    RecommendationList.tsx
  ui/
    Button.tsx
    Card.tsx
    Badge.tsx
    Input.tsx
    Select.tsx
    Textarea.tsx
    DatePicker.tsx   # 커스텀 캘린더(요일 표시, KST 안전, min=오늘)
    Emoji.tsx        # 유니코드 이모지를 TossFace 폰트로 렌더(아이콘 라이브러리 대체)
```

구현 원칙:

- 서버 데이터 조회는 가능한 Server Components에서 처리한다.
- 브라우저 상태가 필요한 컴포넌트만 Client Components로 만든다.
- 폼 제출은 Server Actions를 우선 사용한다.
- Route Handler는 `.ics` 다운로드처럼 파일 응답이 필요한 경우에 사용한다.

## 7. 접근성 및 반응형 기준

- 모든 버튼은 명확한 텍스트 또는 접근 가능한 라벨을 가진다.
- 상태 색상에는 텍스트 라벨을 함께 붙인다.
- 시간표 셀은 키보드 포커스가 가능해야 한다.
- 모바일에서 텍스트가 버튼 밖으로 넘치지 않아야 한다.
- 추천 카드의 핵심 정보는 작은 화면에서도 접히지 않고 먼저 보여야 한다.
- 모바일에서는 주요 CTA 버튼(회의 만들기, 응답 제출 등)을 화면 하단에 고정한다. 데스크톱은 기존 인라인 배치를 유지한다.
- 복사 버튼, 확정 버튼 등 주요 액션은 터치하기 충분한 크기로 만든다.

## 8. 프론트엔드 테스트 시나리오

- 랜딩에서 회의 만들기로 이동할 수 있다.
- 회의 생성 폼에 기본값이 올바르게 들어간다.
- 가상 직원 목록에서 참석자를 검색하고 선택/해제할 수 있다.
- 선택된 참석자 뱃지를 필수참석/선택참석 구역으로 드래그해 참석유형을 변경할 수 있다.
- 공유 화면에서 참석자 링크를 복사할 수 있다.
- 참석자 화면에서 날짜 상태와 공통 시간 적용으로 가능 시간을 지정하고 제출할 수 있다.
- 같은 브라우저에서 제출 후 수정할 수 있다.
- 제출 후 참석자 화면에서 일주일 그리드 캘린더가 표시된다.
- 캘린더 각 칸에 선호(초록)·불가능(빨강) 참석자 이름이 직접 표시되고, 상세 메모 원문은 보이지 않는다.
- admin 화면에서 응답자와 미응답자를 구분해 볼 수 있다.
- admin 화면에서 전체 캘린더로 선호/불가능 집계를 볼 수 있다.
- 추천 후보 카드가 최소 3개 표시된다.
- 필수/선택 변경 시 추천 결과가 다시 계산된다.
- 모든 참석자가 응답하기 전에는 후보 투표가 열리지 않는다.
- 모든 참석자가 후보에 투표하면 최다 득표 후보를 확정할 수 있다. 1위가 동률이면 주최자가 동률 후보 중 하나를 고른다.
- 확정 화면에서 공유 문구 복사와 `.ics` 다운로드가 가능하다.
